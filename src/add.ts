/**
 * 计算任意俩个数字的和。
 * @group 数学
 * @param num1 数
 * @param num2 数
 * @returns 返回一个俩数相加后的和
 * @example
 * ```ts
 * add(1, 1)    //  2
 * add(-1, 1)    //  0
 * ```
 */
export const add = (num1: number, num2: number): number => {
	return num1 + num2;
};
